const timeRanges = ['24h', '7d', '1m', '3m', '1y', 'ytd', 'all'];
const updateInterval = 60000;
const updateIntervalSecs = updateInterval / 1000;

export { timeRanges, updateInterval, updateIntervalSecs };
