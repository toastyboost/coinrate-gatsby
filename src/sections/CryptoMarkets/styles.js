import styled from 'styled-components';
import { Wrap } from 'styles/common';

const BlockWrap = styled(Wrap)``;

const MarketTableContainer = styled.div``;

export { MarketTableContainer, BlockWrap };
